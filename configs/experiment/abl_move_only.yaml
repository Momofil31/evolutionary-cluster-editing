# @package _global_

defaults:
    - override /mutation: default.yaml

mutation:
  mutate_moveOrIsolateOrRemoveCliques:
    # The sum of the following probabilities should be 1.0
    movepb: 1.0
    isolatepb: 0.0
    removepb: 0.0

    # Portion of the individuals to be mutated
    subset_ratio: 0.25

